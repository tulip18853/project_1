#include<stdio.h>
#include<conio.h>
void main(){
    float base,height,area;
    printf("enter the height and base of the triangle ");
    scanf("%f%f",&height,&base);
    area=height*base/2;
    printf("the arae of the triangle is %f",area);
  getch();
} 
